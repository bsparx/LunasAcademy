// Moved to app/(dashboard)/discussion-actions.ts so the site-wide general
// discussion board can share the same post/comment/vote logic. Kept here
// only as a re-export in case anything still imports this path.
export * from "@/app/(dashboard)/discussion-actions";
